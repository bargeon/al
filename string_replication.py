replicate_me = "python"

# * multiplication operator replicates the string
# * multiplication operator only works with integers, attempting to replicate with floating points will produce an error
print(replicate_me * 10)

# * has only two uses for math or string replication otherwise it will produce an error
# print("python" * "python")
